django>=5.0
djangorestframework>=3.15
django-cors-headers>=4.3
psycopg2-binary>=2.9
python-dotenv>=1.0

sqlglot>=25.0.0        # SQL validation (AST parsing)

# LLM providers — chain is Groq -> Mistral (Ollama dropped).
groq>=0.11.0           # primary chat + Whisper speech-to-text
mistralai==1.9.11      # fallback chat